<h1>Editing <%= @song.title %></h1>

<%= form_for(@song)  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :Artist %>
  <%= f.text_field :artist %>
  <br />

  <%= f.submit %>
<% end %>

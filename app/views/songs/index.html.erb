
<% @artists.each do |artist| %>
<%= link_to artist.name, artist_path(artist)%> 
  <% artist.songs.each do |song| %>
  <%=link_to "#{artist.name} - #{song.title}" , song_path(song) %><br>
  <% end %>
<% end %>

<% @artists.each do |artist| %>
<%= link_to artist.name, artist_path(song)%> 
  <% @artist.songs.each do |song| %>
  <%=link_to "#{@artist.name} - #{song.title}" , song_path(song) %><br>
  <% end %>
<% end %>